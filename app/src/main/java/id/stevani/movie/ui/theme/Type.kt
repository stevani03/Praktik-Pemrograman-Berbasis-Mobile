package id.stevani.movie.ui.theme

import androidx.compose.material3.Typography

val Typography = Typography(
    // Atur gaya teks yang diperlukan di sini, misalnya `bodyLarge`, `titleMedium`, dll.
)
